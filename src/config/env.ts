import dotenv from 'dotenv';

// Load the environment variables
dotenv.config();

// TODO: add a validation script for required ENV variables later.

// Export process.env for use throughout the project
export default process.env;
